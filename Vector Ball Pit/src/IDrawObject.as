package {
	import flash.display.Graphics;
	/**
	 * At some point this interface will allow us to use the true power of OOP! :)
	 */
	public interface IDrawObject {
		function drawObject(graphics:Graphics) : void;
	}
}